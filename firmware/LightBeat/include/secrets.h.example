#pragma once

// Copy this file to secrets.h (gitignored) and fill in real values.

const char *DEVICE_ID = "lightbeat-fridge-01";
const char *HEARTBEAT_URL = "https://your-server-url/heartbeat";

struct WifiCredential {
  const char *ssid;
  const char *password;
};

// Tried in order, first one that connects wins. Add/remove entries here to change networks.
const WifiCredential WIFI_NETWORKS[] = {
  { "your-ssid-1", "your-password-1" },
  { "your-ssid-2", "your-password-2" },
};
const int WIFI_NETWORK_COUNT = sizeof(WIFI_NETWORKS) / sizeof(WIFI_NETWORKS[0]);
